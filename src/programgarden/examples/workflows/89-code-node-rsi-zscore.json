{
  "id": "89-code-node-rsi-zscore",
  "name": "CodeNode RSI + Z-Score composite (NASDAQ)",
  "description": "StartNode -> OverseasStockBrokerNode -> OverseasStockHistoricalDataNode(5 NASDAQ names, 1d) -> CodeNode(hand-rolled Wilder RSI + rolling z-score composite signal, stdlib only) -> TableDisplayNode. Educational hand-roll reference for the RSI/ZScore plugins, not a new capability. Defensive on empty data (returns status='no_data'), so it is dry_run-safe. Symbols with fewer than period+1 bars are emitted in 'skipped' (no silent drop).",
  "nodes": [
    {
      "id": "start",
      "type": "StartNode",
      "position": {
        "x": 50,
        "y": 200
      }
    },
    {
      "id": "broker",
      "type": "OverseasStockBrokerNode",
      "credential_id": "stock_cred",
      "position": {
        "x": 250,
        "y": 200
      }
    },
    {
      "id": "historical",
      "type": "OverseasStockHistoricalDataNode",
      "symbols": [
        {
          "symbol": "AAPL",
          "exchange": "NASDAQ"
        },
        {
          "symbol": "MSFT",
          "exchange": "NASDAQ"
        },
        {
          "symbol": "NVDA",
          "exchange": "NASDAQ"
        },
        {
          "symbol": "AMZN",
          "exchange": "NASDAQ"
        },
        {
          "symbol": "GOOGL",
          "exchange": "NASDAQ"
        }
      ],
      "period": "1d",
      "count": 60,
      "position": {
        "x": 500,
        "y": 200
      }
    },
    {
      "id": "rsizscore",
      "type": "CodeNode",
      "data": "{{ nodes.historical.values }}",
      "params": {
        "period": 14,
        "z_window": 20
      },
      "outputs": [
        {
          "name": "signals",
          "type": "array"
        },
        {
          "name": "oversold_count",
          "type": "number"
        },
        {
          "name": "skipped",
          "type": "array"
        },
        {
          "name": "status",
          "type": "string"
        }
      ],
      "code": "async def execute(data, params, context):\n    import statistics\n    period = int(params.get('period', 14))\n    z_window = int(params.get('z_window', 20))\n    rows = [r for r in (data or []) if isinstance(r, dict)]\n    if not rows:\n        return {'signals': [], 'oversold_count': 0, 'skipped': [], 'status': 'no_data'}\n    signals = []\n    skipped = []\n    for row in rows:\n        ts = row.get('time_series') or []\n        closes = [b.get('close') for b in ts if isinstance(b, dict) and b.get('close') is not None]\n        if len(closes) < period + 1:\n            skipped.append({'symbol': row.get('symbol'), 'exchange': row.get('exchange'),\n                            'reason': 'insufficient_bars'})\n            continue\n        gains = 0.0\n        losses = 0.0\n        for i in range(1, period + 1):\n            diff = closes[i] - closes[i - 1]\n            if diff >= 0:\n                gains += diff\n            else:\n                losses -= diff\n        avg_gain = gains / period\n        avg_loss = losses / period\n        for i in range(period + 1, len(closes)):\n            diff = closes[i] - closes[i - 1]\n            gain = diff if diff > 0 else 0.0\n            loss = -diff if diff < 0 else 0.0\n            avg_gain = (avg_gain * (period - 1) + gain) / period\n            avg_loss = (avg_loss * (period - 1) + loss) / period\n        if avg_loss == 0:\n            rsi = 100.0\n        else:\n            rsi = 100.0 - 100.0 / (1.0 + avg_gain / avg_loss)\n        window = closes[-z_window:] if len(closes) >= z_window else closes\n        mean = statistics.fmean(window)\n        sd = statistics.pstdev(window)\n        zscore = (closes[-1] - mean) / sd if sd else 0.0\n        if rsi < 30 and zscore < -1:\n            sig = 'oversold'\n        elif rsi > 70 and zscore > 1:\n            sig = 'overbought'\n        else:\n            sig = 'neutral'\n        signals.append({'symbol': row.get('symbol'), 'exchange': row.get('exchange'),\n                        'rsi': round(rsi, 2), 'zscore': round(zscore, 2), 'signal': sig})\n    signals.sort(key=lambda r: r['rsi'])\n    oversold = sum(1 for s in signals if s['signal'] == 'oversold')\n    return {'signals': signals, 'oversold_count': oversold, 'skipped': skipped,\n            'status': 'ok' if signals else 'insufficient_data'}",
      "position": {
        "x": 820,
        "y": 200
      }
    },
    {
      "id": "display",
      "type": "TableDisplayNode",
      "data": "{{ nodes.rsizscore.signals }}",
      "position": {
        "x": 1120,
        "y": 200
      }
    }
  ],
  "edges": [
    {
      "from": "start",
      "to": "broker"
    },
    {
      "from": "broker",
      "to": "historical"
    },
    {
      "from": "historical",
      "to": "rsizscore"
    },
    {
      "from": "rsizscore",
      "to": "display"
    }
  ],
  "credentials": [
    {
      "credential_id": "stock_cred",
      "type": "broker_ls_overseas_stock",
      "data": [
        {
          "key": "appkey",
          "value": "",
          "type": "password",
          "label": "App Key"
        },
        {
          "key": "appsecret",
          "value": "",
          "type": "password",
          "label": "App Secret"
        }
      ]
    }
  ]
}
